import { C as Convention, B as BodyLongitudes, E as EphemerisEngine } from './types-Cm5gDU--.js';
export { P as PlanetKey } from './types-Cm5gDU--.js';

/**
 * Apparent ecliptic longitude of the Sun, in degrees. Cheap path for the
 * design-arc search (which only needs the Sun); apparent matches the body
 * convention so the 88° solar arc is computed consistently.
 */
declare function sunLongitude(jdUT: number): number;
declare function computeBodyLongitudes(jdUT: number, convention?: Convention): BodyLongitudes;
/**
 * The high-accuracy engine. Sub-arcsecond on every direct body, so base is
 * meaningful given a to-the-second birth time.
 *
 * Importing this links GPL-3.0 code. See NOTICE.md.
 */
declare function createMoshierEngine(): EphemerisEngine;

export { BodyLongitudes, Convention, EphemerisEngine, computeBodyLongitudes, createMoshierEngine, sunLongitude };
